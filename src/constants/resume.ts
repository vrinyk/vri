/**
 * The CV lives in /public so Vite serves it verbatim at a stable URL —
 * importing it through the bundler would rename it with a content hash.
 */
export const RESUME_URL = "/Vrinda-Khandelwal-Resume-v5.pdf";

/** Filename the browser saves it as. */
export const RESUME_FILE = "Vrinda-Khandelwal-Product-Designer.pdf";
